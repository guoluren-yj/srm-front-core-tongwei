const borderOpacity = '0.15'; // 边框颜色透明度a
const backgroundOpacity = '0.1'; // 背景颜色透明度a

const A = ['245', '99', '73'];
const B = ['252', '160', '0'];
const C = ['71', '184', '129'];
const D = ['54', '194', '207'];
const E = ['48', '149', '242'];
const F = ['63', '91', '181'];
const G = ['0', '0', '0'];

export default {
  A: {
    'label-preview-color': `rgba(${A.join(',')},1)`, // 预览标签颜色
    'label-preview-border-color': `rgba(${A.join(',')},${borderOpacity})`, // 预览标签边框颜色透明度a
    'label-preview-background-color': `rgba(${A.join(',')},${backgroundOpacity})`, // 预览标签背景颜色透明度a
  },
  B: {
    'label-preview-color': `rgba(${B.join(',')},1)`, // 预览标签颜色
    'label-preview-border-color': `rgba(${B.join(',')},${borderOpacity})`, // 预览标签边框颜色透明度a
    'label-preview-background-color': `rgba(${B.join(',')},${backgroundOpacity})`, // 预览标签背景颜色透明度a
  },
  C: {
    'label-preview-color': `rgba(${C.join(',')},1)`, // 预览标签颜色
    'label-preview-border-color': `rgba(${C.join(',')},${borderOpacity})`, // 预览标签边框颜色透明度a
    'label-preview-background-color': `rgba(${C.join(',')},${backgroundOpacity})`, // 预览标签背景颜色透明度a
  },
  D: {
    'label-preview-color': `rgba(${D.join(',')},1)`, // 预览标签颜色
    'label-preview-border-color': `rgba(${D.join(',')},${borderOpacity})`, // 预览标签边框颜色透明度a
    'label-preview-background-color': `rgba(${D.join(',')},${backgroundOpacity})`, // 预览标签背景颜色透明度a
  },
  E: {
    'label-preview-color': `rgba(${E.join(',')},1)`, // 预览标签颜色
    'label-preview-border-color': `rgba(${E.join(',')},${borderOpacity})`, // 预览标签边框颜色透明度a
    'label-preview-background-color': `rgba(${E.join(',')},${backgroundOpacity})`, // 预览标签背景颜色透明度a
  },
  F: {
    'label-preview-color': `rgba(${F.join(',')},1)`, // 预览标签颜色
    'label-preview-border-color': `rgba(${F.join(',')},${borderOpacity})`, // 预览标签边框颜色透明度a
    'label-preview-background-color': `rgba(${F.join(',')},${backgroundOpacity})`, // 预览标签背景颜色透明度a
  },
  G: {
    'label-preview-color': `rgba(${G.join(',')},1)`, // 预览标签颜色
    'label-preview-border-color': `rgba(${G.join(',')},${borderOpacity})`, // 预览标签边框颜色透明度a
    'label-preview-background-color': `rgba(${G.join(',')},${backgroundOpacity})`, // 预览标签背景颜色透明度a
  },
};
