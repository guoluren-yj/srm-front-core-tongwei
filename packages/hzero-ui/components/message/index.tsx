import type { ConfigOnClose, ConfigOptions, ThenableArgument, MessageType } from 'choerodon-ui/lib/message';

export { default } from 'choerodon-ui/lib/message';
export type { ConfigOnClose, ConfigOptions, ThenableArgument, MessageType };
