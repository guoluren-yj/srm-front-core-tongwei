import Form from './Form';
import type { FormProps, FormComponentProps, FormCreateOption, ValidateCallback, ValidationRule } from './Form';
import type { FormItemProps } from './FormItem';

export type { FormProps, FormComponentProps, FormCreateOption, ValidateCallback, ValidationRule, FormItemProps };

export default Form;
